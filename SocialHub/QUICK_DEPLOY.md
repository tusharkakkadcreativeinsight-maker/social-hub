# Quick Deploy Guide - SocialHub on Render.com

## 🚀 Deploy in 3 Steps

### Step 1: Push to GitHub (2 minutes)

```bash
cd "d:\social media\SocialHub"

# Initialize git if not already done
git init
git add .
git commit -m "Initial commit with Render deployment config"

# Create repository on GitHub first, then:
git remote add origin https://github.com/YOUR_USERNAME/socialhub.git
git branch -M main
git push -u origin main
```

### Step 2: Deploy on Render (5 minutes)

1. **Go to**: https://dashboard.render.com/blueprints
2. **Click**: "New Blueprint"
3. **Connect** your GitHub repository
4. **Select** your SocialHub repository
5. **Click**: "Apply"

Render will automatically:
- Create PostgreSQL database
- Deploy backend API
- Deploy frontend static site
- Configure all environment variables

### Step 3: Initialize Database (1 minute)

After deployment completes:

1. Go to your backend service (socialhub-api)
2. Click "Shell" tab
3. Run:
```bash
cd backend
python -m alembic upgrade head
```

## ✅ Your App is Live!

Visit your URLs:
- **Frontend**: https://socialhub-frontend.onrender.com
- **Backend API**: https://socialhub-api.onrender.com
- **API Docs**: https://socialhub-api.onrender.com/docs

## 🔑 Create Admin User

In Render shell, run:
```bash
python -c "
from app.database import SessionLocal
from app.models.models import User
from app.utils.security import hash_password

db = SessionLocal()
admin = User(
    email='admin@socialhub.com',
    username='admin',
    hashed_password=hash_password('Admin123!'),
    full_name='Admin User',
    role='admin',
    is_active=True,
    is_banned=False,
    is_email_verified=True
)
db.add(admin)
db.commit()
print('Admin created!')
"
```

## 📝 Important Notes

1. **Free Tier**: Backend sleeps after 15 min inactivity (wakes in ~30 sec)
2. **Database**: Free tier expires after 90 days (renewable)
3. **Files**: Uploaded files are lost on restart (use S3 for production)
4. **SECRET_KEY**: Already generated by Render (auto-configured)

## 🔧 If Something Goes Wrong

Check logs: Render Dashboard → Your Service → "Logs" tab

Common issues:
- **Database connection**: Verify DATABASE_URL is set
- **Build fails**: Check requirements.txt is in root
- **Frontend blank**: Check rewrite rules in static site settings

## 📚 Full Documentation

See `DEPLOYMENT.md` for detailed instructions and `DEPLOYMENT_CHECKLIST.md` for complete checklist.

## 🎯 Next Steps

1. Test all features (register, login, posts, etc.)
2. Configure custom domain (optional)
3. Set up email (optional)
4. Add OpenAI API key for AI chat (optional)
5. Upgrade to paid plan ($7/month) to avoid sleep

---

**Need Help?**
- Render Docs: https://render.com/docs
- Check DEPLOYMENT.md for troubleshooting